<h1>Hello Alligators</h1>

<p>
<%= @name %>
</p>

<%= select_tag :items, nil, {id: 'items'} %>
<%= button_tag :Next, {id: 'next' } %>
<br/>
<br/>

<table>
  <thead>
    <tr>name</tr>
  </thead>
  <% @urls.each do  |url| %>
    <tr>
      <td>
      <%= url.name %>
      <td>
    </tr>
  <% end %>
</table>
